var answer = prompt('What is the highest mountain in the state of Colorado?');
if ( answer.toUpperCase() === 'MT ELBERT' ) {
  document.write("<p>That's right!</p>");
} else {
  document.write("<p>That's incorrect.</p>");
}
